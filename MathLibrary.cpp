#include "MathLibrary.h"

std::vector<float> operator+(std::vector<float> &other)
{
	
}

std::vector<float> Normalize(float a_x, float a_y)
{

}

std::vector<float> Normalize(float a_x, float a_y, float a_z)
{

}

float Magnitude(float a, float b)
{
	float c = sqrt((a*a) + (b*b));
	return c;
}

